export const selectToken = (state) => state.user.token;
export const selectUser = (state) => state.user;
export const selectMyProduct = (state) => state.user.product;
export const selectMyBid = (state) => state.user.bid;
